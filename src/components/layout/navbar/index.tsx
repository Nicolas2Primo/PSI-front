const Navbar = () => {
  return (
    <div className="fixed flex items-center justify-between w-screen h-16 text-white bg-black px-7">
      <span>Logo</span>

      <div className="flex gap-8 w-fit">
        <button>Home</button>
        <button>Home</button>
        <button>Home</button>
      </div>
    </div>
  );
};

export default Navbar;
